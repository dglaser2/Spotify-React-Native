import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        margin: 10,
    },
    title: {
        color: '#EE4141',
        fontSize: 24,
        fontWeight: 'bold',
        margin: 10
    }
    // image: {
    //     width: '100%',
    //     height: 125,
    // },
})

export default styles;